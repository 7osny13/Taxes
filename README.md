# نظام متابعة إيصالات الـ 1% الضريبية

نظام شامل لإدارة ومتابعة إيصالات الضريبة بنسبة 1% للفواتير الصادرة للشركات.

## 🌟 المميزات الرئيسية

### 📋 إدارة الفواتير
- تسجيل الفواتير بكل تفاصيلها (رقم - تاريخ - شركة - مبلغ - قيمة 1%)
- حساب تلقائي لقيمة الـ 1%
- حالة كل فاتورة (معلقة - مستلمة - متأخرة)
- عداد تلقائي للأيام المتبقية

### 🔔 نظام التنبيهات
- تنبيه بعد 40 يوم (تحذير أولي)
- تنبيه بعد 50 يوم (متأخر)
- لوحة تنبيهات خاصة تعرض جميع الفواتير المتأخرة

### 🏢 إدارة الشركات
- قاعدة بيانات شاملة للشركات (+100 شركة)
- بيانات الاتصال الكاملة
- سجل التعاملات لكل شركة
- إحصائيات الفواتير المعلقة لكل شركة

### 📊 التقارير
- تقارير شهرية شاملة
- تقارير سنوية
- إحصائيات مفصلة (مستلم - معلق - متأخر)
- ملخص بالشركات وحالة فواتيرها
- طباعة مباشرة للتقارير
- تصدير إلى CSV

### 📎 رفع الإيصالات
- رفع صور الإيصالات المختومة (JPG, PNG, PDF)
- معاينة الإيصالات المرفوعة
- حفظ الإيصالات في قاعدة البيانات
- عرض الإيصالات في أي وقت

### 🔍 البحث والفلترة
- بحث بالفواتير (رقم - شركة)
- فلترة حسب الحالة
- فلترة حسب الشهر
- بحث في الشركات

## 🚀 طريقة التنصيب

### 1. إنشاء حساب Supabase

1. اذهب إلى [supabase.com](https://supabase.com)
2. سجل حساب جديد مجاناً
3. أنشئ مشروع جديد (New Project)

### 2. إنشاء الجداول

قم بتنفيذ الأوامر التالية في SQL Editor في Supabase:

```sql
-- جدول الشركات
CREATE TABLE companies (
    id BIGSERIAL PRIMARY KEY,
    name TEXT NOT NULL,
    tax_id TEXT,
    phone TEXT,
    email TEXT,
    address TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- جدول الفواتير
CREATE TABLE invoices (
    id BIGSERIAL PRIMARY KEY,
    number TEXT NOT NULL,
    date DATE NOT NULL,
    company_id BIGINT REFERENCES companies(id),
    amount DECIMAL(15,2) NOT NULL,
    tax_amount DECIMAL(15,2) NOT NULL,
    notes TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- جدول الإيصالات
CREATE TABLE receipts (
    id BIGSERIAL PRIMARY KEY,
    invoice_id BIGINT REFERENCES invoices(id) ON DELETE CASCADE,
    receipt_date DATE NOT NULL,
    file_name TEXT NOT NULL,
    file_type TEXT NOT NULL,
    file_data TEXT NOT NULL,
    notes TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- إنشاء فهارس لتحسين الأداء
CREATE INDEX idx_invoices_company ON invoices(company_id);
CREATE INDEX idx_invoices_date ON invoices(date);
CREATE INDEX idx_receipts_invoice ON receipts(invoice_id);
```

### 3. تفعيل RLS (Row Level Security)

```sql
-- تفعيل RLS على الجداول
ALTER TABLE companies ENABLE ROW LEVEL SECURITY;
ALTER TABLE invoices ENABLE ROW LEVEL SECURITY;
ALTER TABLE receipts ENABLE ROW LEVEL SECURITY;

-- إنشاء سياسات للسماح بجميع العمليات (للاستخدام الشخصي)
CREATE POLICY "Enable all operations for companies" ON companies
    FOR ALL USING (true) WITH CHECK (true);

CREATE POLICY "Enable all operations for invoices" ON invoices
    FOR ALL USING (true) WITH CHECK (true);

CREATE POLICY "Enable all operations for receipts" ON receipts
    FOR ALL USING (true) WITH CHECK (true);
```

### 4. الحصول على بيانات الاتصال

1. في Supabase، اذهب إلى Settings → API
2. انسخ:
   - `Project URL` (SUPABASE_URL)
   - `anon public` key (SUPABASE_ANON_KEY)

### 5. تحديث ملف config.js

افتح ملف `js/config.js` وضع بياناتك:

```javascript
const SUPABASE_URL = 'YOUR_SUPABASE_URL';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
```

### 6. رفع الملفات على GitHub

```bash
# إنشاء repository جديد على GitHub
git init
git add .
git commit -m "Initial commit: Tax Receipts Tracker"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/tax-receipts-tracker.git
git push -u origin main
```

### 7. تفعيل GitHub Pages

1. اذهب إلى Settings → Pages
2. اختر Branch: `main`
3. اختر المجلد: `/ (root)`
4. احفظ واضغط Save

سيكون الموقع متاحاً على:
```
https://YOUR_USERNAME.github.io/tax-receipts-tracker/
```

## 📱 طريقة الاستخدام

### إضافة شركة جديدة
1. اذهب إلى تبويب "الشركات"
2. اضغط "+ شركة جديدة"
3. املأ البيانات (الاسم والرقم الضريبي إلزاميان)
4. احفظ

### إضافة فاتورة جديدة
1. اذهب إلى تبويب "الفواتير"
2. اضغط "+ فاتورة جديدة"
3. املأ بيانات الفاتورة
4. اختر الشركة من القائمة
5. أدخل المبلغ الإجمالي (سيتم حساب 1% تلقائياً)
6. احفظ

### رفع إيصال
1. ابحث عن الفاتورة في قائمة الفواتير
2. اضغط زر "رفع" في عمود الإيصال
3. اختر تاريخ استلام الإيصال
4. ارفع صورة أو PDF للإيصال المختوم
5. احفظ

### عرض التقارير
1. اذهب إلى تبويب "التقارير"
2. اختر الشهر والسنة
3. اضغط "تقرير شهري"
4. يمكنك طباعة التقرير مباشرة

### متابعة التنبيهات
1. اذهب إلى تبويب "التنبيهات"
2. شاهد جميع الفواتير المتأخرة أو القريبة من الموعد النهائي
3. يمكنك رفع الإيصال مباشرة من صفحة التنبيهات

## 🎨 الواجهة

- تصميم حديث وسهل الاستخدام
- متوافق مع جميع الشاشات (موبايل - تابلت - كمبيوتر)
- ألوان متدرجة جميلة
- تنبيهات ملونة حسب الحالة:
  - 🟢 أخضر: مستلم
  - 🟡 أصفر: معلق
  - 🔴 أحمر: متأخر

## 🔒 الأمان

- جميع البيانات محفوظة في Supabase (قاعدة بيانات سحابية آمنة)
- نظام Row Level Security (RLS) مفعل
- الملفات المرفوعة محفوظة بصيغة Base64 في قاعدة البيانات

## 💡 نصائح الاستخدام

1. **سجل الشركات أولاً**: قبل إضافة الفواتير، سجل جميع الشركات التي تتعامل معها
2. **تابع التنبيهات يومياً**: راجع تبويب التنبيهات بشكل منتظم لتجنب التأخير
3. **استخدم التقارير الشهرية**: في نهاية كل شهر، اطبع التقرير للمراجعة والأرشفة
4. **احفظ نسخة احتياطية**: يمكنك تصدير البيانات من Supabase بشكل دوري

## 🆘 المساعدة والدعم

### المشاكل الشائعة:

**المشكلة**: البيانات لا تظهر
**الحل**: تأكد من:
- صحة SUPABASE_URL و SUPABASE_ANON_KEY
- تنفيذ جميع أوامر SQL
- تفعيل RLS Policies

**المشكلة**: لا يمكن رفع الإيصالات
**الحل**: تأكد من:
- حجم الملف أقل من 5MB
- صيغة الملف مدعومة (JPG, PNG, PDF)

**المشكلة**: التنبيهات لا تعمل
**الحل**: تأكد من تسجيل تاريخ الفاتورة بشكل صحيح

## 📊 الإحصائيات

النظام يعرض إحصائيات فورية في الصفحة الرئيسية:
- إجمالي الفواتير
- الفواتير المستلمة
- الفواتير المعلقة
- الفواتير المتأخرة

## 🔄 التحديثات المستقبلية

- [ ] إضافة نظام إشعارات عبر البريد الإلكتروني
- [ ] إضافة رسوم بيانية للإحصائيات
- [ ] إضافة نظام مستخدمين متعدد
- [ ] إضافة تصدير للتقارير بصيغة PDF
- [ ] إضافة نظام نسخ احتياطي تلقائي

## 📝 الترخيص

هذا المشروع مجاني للاستخدام الشخصي والتجاري.

## 👨‍💻 التطوير

تم تطوير هذا النظام باستخدام:
- HTML5
- CSS3
- JavaScript (Vanilla)
- Supabase (قاعدة البيانات)

---

صنع بـ ❤️ لإدارة أفضل لأعمالك
